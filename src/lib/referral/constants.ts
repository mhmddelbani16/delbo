// Central config for the referral growth loop (P1 feature).
// Change these to test different reward structures without touching
// application logic elsewhere.

export const REFERRALS_REQUIRED = 5;
export const REFERRAL_REWARD = 5; // bonus AI tailorings unlocked

export const REFERRAL_COOKIE_NAME = "delbo_ref";
export const REFERRAL_COOKIE_MAX_AGE_DAYS = 30;
